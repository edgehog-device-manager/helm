{{- if .Values.forwarder.enabled }}
{{- $fullname := include "edgehog.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullname }}-device-forwarder
  labels:
    {{- include "edgehog.componentLabels" (dict "root" . "component" "device-forwarder") | nindent 4 }}
spec:
  replicas: {{ .Values.forwarder.replicaCount }}
  selector:
    matchLabels:
      {{- include "edgehog.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: device-forwarder
  template:
    metadata:
      {{- with .Values.forwarder.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "edgehog.componentLabels" (dict "root" . "component" "device-forwarder") | nindent 8 }}
        {{- with .Values.forwarder.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.forwarder.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: edgehog-device-forwarder
          image: {{ include "edgehog.image" (dict "root" . "image" .Values.forwarder.image "defaultTag" "0.1.0") }}
          imagePullPolicy: {{ .Values.forwarder.image.pullPolicy }}
          {{- with .Values.forwarder.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: RELEASE_NAME
              value: edgehog-device-forwarder
            - name: PORT
              value: {{ .Values.forwarder.containerPort | quote }}
            - name: PHX_HOST
              value: {{ required "forwarder.host is required when forwarder.enabled is true" .Values.forwarder.host | quote }}
            - name: SECRET_KEY_BASE
              {{- if .Values.forwarder.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.forwarder.existingSecret }}
                  key: {{ .Values.forwarder.existingSecretKey }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}
                  key: forwarder-{{ .Values.forwarder.existingSecretKey }}
              {{- end }}
            {{- with .Values.forwarder.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.forwarder.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.forwarder.containerPort }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.forwarder.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.forwarder.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.forwarder.resources | nindent 12 }}
      {{- with .Values.forwarder.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.forwarder.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.forwarder.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-device-forwarder
  labels:
    {{- include "edgehog.componentLabels" (dict "root" . "component" "device-forwarder") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4000
      targetPort: http
      protocol: TCP
  selector:
    {{- include "edgehog.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: device-forwarder

{{- end }}
